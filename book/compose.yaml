services:
  book:
    build: .
    image: rth-book
    container_name: rth-book
    ports:
      - "80:80"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/run